description = "Validate workflow registries and state"
prompt = "Run: node scripts/validate/validate.mjs. Also: node scripts/bootstrap/bootstrap.mjs. Report: duplicate IDs, broken refs, missing specs, invalid states, invalid transitions."
