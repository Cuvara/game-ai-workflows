description = "Find feature by ID, name, or fuzzy match"
prompt = "Run: node adapters/generic/agent-workflow.mjs find-feature {{args}}. If ambiguous show candidates and ask user. Never silently guess."
