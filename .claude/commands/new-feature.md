Create new feature with stable ID and scaffolding

Read skills/new-feature/SKILL.md. Check duplicates: node adapters/generic/agent-workflow.mjs find-feature {{args}}. If exists stop. Propose stable ID (category.name format). Ask user to confirm. Create docs/features/<id>/ dir, spec from contracts/FEATURE_SPEC.md, add to docs/registry/features.yaml as PLANNED. Never create without user confirmation.
