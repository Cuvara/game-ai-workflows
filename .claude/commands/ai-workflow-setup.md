Bootstrap the game AI workflow environment.

Read `skills/bootstrap/SKILL.md` for the full procedure.

Execute: `node scripts/bootstrap/bootstrap.mjs`

If bootstrap reports issues, help the user resolve them.
If Google Drive MCP is missing and user needs it, guide setup per `docs/setup/google-drive.md`.
